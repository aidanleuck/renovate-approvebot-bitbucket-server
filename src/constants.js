/**
 * Constants shared across the application
 */

const MANUAL_MERGE_MESSAGE = '**Automerge**: Disabled by config.';
const AUTO_MERGE_MESSAGE = '**Automerge**: Enabled.';

module.exports = {
  MANUAL_MERGE_MESSAGE,
  AUTO_MERGE_MESSAGE,
};
